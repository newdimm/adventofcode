#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void print_int(int number)
{
}

int main(int argc, char *argv[])
{
    int number = 234;
    if (agrc > 1)
    {
        number = atoi(argv[1]);
    }

    print_int(number);

    return 0;
}
